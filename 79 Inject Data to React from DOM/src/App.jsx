import React from "react";

export default function App({ userId, theme }) {
  return (
    <div>
      <h1>Welcome to React App</h1>
      <p>userId = {userId}</p>
      <p>theme = {theme}</p>
    </div>
  );
}
